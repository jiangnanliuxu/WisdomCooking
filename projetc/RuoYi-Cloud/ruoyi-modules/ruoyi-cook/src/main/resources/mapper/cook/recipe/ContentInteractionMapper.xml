<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.cook.recipe.mapper.ContentInteractionMapper">

    <resultMap id="ContentInteractionResult" type="com.ruoyi.cook.recipe.domain.ContentInteraction">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="targetType" column="target_type"/>
        <result property="targetId" column="target_id"/>
        <result property="actionType" column="action_type"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectOne" resultMap="ContentInteractionResult">
        select id, user_id, target_type, target_id, action_type, status, created_at, updated_at
        from cook_content_interactions
        where user_id = #{userId}
          and target_type = #{targetType}
          and target_id = #{targetId}
          and action_type = #{actionType}
        limit 1
    </select>

    <insert id="insertInteraction" parameterType="com.ruoyi.cook.recipe.domain.ContentInteraction" useGeneratedKeys="true" keyProperty="id">
        insert into cook_content_interactions (user_id, target_type, target_id, action_type, status)
        values (#{userId}, #{targetType}, #{targetId}, #{actionType}, #{status})
    </insert>

    <update id="updateStatus">
        update cook_content_interactions
        set status = #{status},
            updated_at = current_timestamp
        where id = #{id}
    </update>

</mapper>
